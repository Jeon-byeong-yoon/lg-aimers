# V30–V31 일반화 개선 실험

## V30: 구간형 Logistic

연속형 피처를 분위수 구간으로 변환한 규제 Logistic을 V25에 소량 혼합했다. 2023은 개선됐지만 2024는 최소 가중치에서도 악화되어 제출 후보에서 제외했다.

- 두 시즌 동시 개선 조합: 0개
- 제출 ZIP 생성하지 않음

## V31: V25 피처 제거

V25 form/context HGB에서 일반화가 불안정할 수 있는 피처 그룹을 제거하고 2022·2023·2024 시간순 OOF로 비교했다.

최종 조합:

- form HGB: Trackman 표준편차 피처 제거
- context HGB: `pitcher_batter` matchup HTE 제거
- V25의 모델 가중치와 보정 방식은 그대로 유지

## 검증 결과

| 검증 시즌 | V25 대비 Brier 개선 |
|---|---:|
| 2022 raw OOF | 0.000008927 |
| 2023 expanding calibration | 0.000052545 |
| 2024 expanding calibration | 0.000008542 |

- 세 시즌 동시 개선
- V25 예측과의 상관: 0.9983–0.9994
- 새로운 세부 잔차 lookup은 추가하지 않음

## 제출 파일

- `submit_v31.zip`
- ZIP 무결성 검사 통과
- 격리 환경 실행 통과
- 출력 row_id 유일성, 결측, 확률 범위 검사 통과

## 공개 제출 결과

- V31 Public Score: `891.3006188625`
- V25 Public Score: `889.9158629160`
- V25 대비 개선: `+1.3847559465`
- 판정: **채택**, 현재 기준선을 `submit_v31.zip`으로 변경

불안정 피처를 제거한 보수적 개선이 공개 리더보드에서도 재현됐다. 이후 실험은 V31을 기준으로 비교한다.

모든 Target Encoding과 보정 lookup은 공식 학습 데이터의 이전 시즌 OOF 또는 전체 학습 데이터로 고정한다. 테스트 전체 통계나 테스트 행 간 관계는 사용하지 않는다.
