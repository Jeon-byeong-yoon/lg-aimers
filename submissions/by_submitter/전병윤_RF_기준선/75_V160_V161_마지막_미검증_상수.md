# V160–V161 — 마지막 미검증 상수에 여지가 있었다

기준선: **V156, Public 1052.1807** (최고점)

---

## 1. 왜 남아 있었는가

`RELIABILITY_SCALE`은 시즌내 열 하나를 만든다.

```
ins_pitcher_reliability = inside_n / (inside_n + reliability_scale)
```

150은 **믿음의 절반 지점을 현재 시즌 150투구**로 놓은 것이고 V92가 사전에 정했다.

V155가 **드리프트 항의** 같은 상수를 18조합 훑어 150이 최적임을 확인했다. 그런데
드리프트 항은 복원을 **shrinkage 3**에서 읽어 그 열을 **곱한다**. 반면 Form(0.32) ·
신경망(0.20) · FM(0.07)은 **shrinkage 20**에서 **입력으로** 읽는다.
**다른 질문이고, 첫 번째만 물어봤던 것이다.**

### 범위 설계 — CatBoost를 빼서 비용을 4분의 1로

CatBoost도 그 열을 읽지만 **이미 자기 복원을 따로 받는다** — V154가 투영 사전확률을
줬고 Form과 두 신경망은 통산 사전확률을 유지했다. 그래서 CatBoost는 150을 유지하고
재학습하지 않는다. 폴드당 350초가 빠져 배율당 24분이 7분이 된다.

**그리고 배포에도 이득이다.** 상수가 **이미 두 번 계산되는 블록 안의 한 열**만 바꾸므로
네 번째 복원이 필요 없다.

### 통제 — 각 성분의 원래 사전확률 관례를 쓴다

V158에서 통제가 실패한 원인이 사전확률 범위였다. 그래서 각 성분이 원래 쓴 관례를
그대로 썼다 — **Form은 폴드별**(V102), **신경망·FM은 전역**(V112·V130).

```
control at reliability 150 versus the stored predictions:
  form           max abs difference 0.000e+00
  network        max abs difference 0.000e+00
  factorization  max abs difference 0.000e+00
```

**세 성분 모두 완벽히 재현했다.** 전역 사전확률의 미세 누출은 네 배율에 **동일하게**
들어가므로 비교를 움직이지 못한다. 그 정리는 점수가 아니라 추정 정직성의 문제이고
별건이다.

---

## 2. V160 결과 — 300이 안전하다

| reliability | min | 평균 | 2022 | 2023 | 2024 | 블록 | |
|---|---|---|---|---|---|---|---|
| 75 | −0.82 | −0.63 | −0.35 | −0.82 | −0.72 | 40% | |
| **150** (현행) | 0 | 0 | 0 | 0 | 0 | — | 기준 |
| **300** | **+0.54** | **+1.56** | **+0.54** | **+3.37** | **+0.77** | **65%** | **SAFE** |
| 600 | −0.25 | +2.04 | +0.64 | +5.74 | −0.25 | 60% | |

`ins_pitcher_reliability` 평균은 `75 → 0.7936`, `150 → 0.6963`, `300 → 0.5761`,
`600 → 0.4425`.

### Form은 이 열을 완전히 무시한다

| reliability | Form 2022 | 2023 | 2024 |
|---|---|---|---|
| 75 | 2392 | −754 | 789 |
| 150 | 2392 | −754 | 789 |
| 300 | 2392 | −754 | 789 |
| 600 | 2392 | −754 | 789 |

**네 배율에서 예측이 완전히 동일하다.** 105개 피처 중 하나이고 HGB가 다른 열로 같은
정보를 얻으니 분할에 쓰지 않는다. **움직임은 전부 두 신경망에서 온다** —
신경망 2024가 `618 → 616 → 629 → 621`, FM이 `162 → 147 → 155 → 148`.

임베딩 신경망은 피처를 선택하지 않고 전부 dense layer에 넣으므로, 한 열의 스케일 변화가
트리 모델과 달리 실제로 전달된다.

---

## 3. V161 제출본

| | V156 (1052.1807) | **V161** |
|---|---|---|
| 블렌드 가중치 | — | **변경 없음** |
| 보정 세그먼트 | — | **변경 없음** |
| Form · 신경망 · FM | reliability **150** | reliability **300** (재학습) |
| CatBoost | 자기 블록, reliability 150 | **그대로** |
| Context | — | **그대로** (시즌내 블록을 안 받음) |

보정 shift `-0.010674754699` (V156 `-0.010678264995`).
Context 모델과 2025 Trackman lookup은 기존 번들에서 그대로 옮겼다.

추론에서 복원은 여전히 **세 번**이다 — shrinkage 20 통산 **reliability 300**
(Form · 신경망 · FM), shrinkage 20 투영 **reliability 150**(CatBoost),
shrinkage 3 통산 reliability 150(드리프트 항). 세 값 모두 보정 번들에서 읽는다.

### 검증

| 항목 | 결과 |
|---|---|
| ZIP | `submit_v161.zip`, 73.0 MiB, md5 `993d49754403a9586ad392ed091c0ea6` |
| 서버 로드 게이트 | 10/10 |
| 공식 5행 | 통과, `0.441907` |
| **행 독립성** | **`0.000e+00`** |
| 예측 분포 | mean `0.511274`, `[0.246953, 0.718930]` |

기대치는 말하지 않는다 — 제출 일곱 번에서 크기 비율이 `0.13`~`2.15`로 흩어졌고
예측 규칙이 없다. **손실 폴드가 없다는 것만이 근거다.**

---

## 4. 손대지 않은 상수 최종 집계

| 상수 | 결과 |
|---|---|
| 드리프트 `reliability_scale` | 18조합 전부 손실 폴드 (V155) |
| 경험 세그먼트 smoothing · 가중 | 15조합 전부 손실 폴드 (V155) |
| **경험 구간 경계** | **개선 → V156 `+1.33`** |
| Context 하이퍼파라미터 5개 | 일곱 변형 전부 2024 음수 (V157) |
| target · 계층 인코딩 strength | 절반~4배 전부 2024 음수 (V158) |
| **피처 쪽 `RELIABILITY_SCALE`** | **개선 → V161 (제출 대기)** |

**열한 개 중 두 개가 개선 가능했다.** 둘 다 **근거가 가장 약한 쪽**이었다 —
구간 경계는 진단 표를 읽기 좋게 고른 값이었고, 피처 쪽 reliability는 드리프트 쪽만
검증하고 넘어간 값이었다.

---

## 5. 결과 — 1052.1807 → **1053.2326** (`+1.05`), 최고점 갱신

`submit_v161.zip`, md5 `993d49754403a9586ad392ed091c0ea6`.

| 제출 | min 시즌 | 3시즌 평균 | 실제 | 평균 비율 |
|---|---|---|---|---|
| V117 | +9.9 | +31.4 | +25.67 | 0.82 |
| V122 | +5.01 | +10.35 | +18.80 | 1.82 |
| V138 | +0.64 | +25.08 | +3.51 | 0.14 |
| V144 | −1.86 | +8.43 | −1.14 | 음수 |
| V151 | −32.36 | — | −2.44 | — |
| V154 | +2.29 | +2.34 | +0.30 | 0.13 |
| V156 | +0.00 | +0.62 | +1.33 | 2.15 |
| **V161** | **+0.54** | **+1.56** | **+1.05** | **0.67** |

**최소 시즌은 부호를 8/8 맞혔다.** 최소가 0 이상이면 올랐고, 음수면 내렸다. 예외가 없다.

크기는 여전히 예측 불가다 — 평균 비율이 `0.13`~`2.15`, 이번엔 `0.67`이다.
V156에서 폐기한 크기 문턱을 다시 세울 근거는 없다.

### 이 구간의 정리

세 층에 걸쳐 손대지 않은 상수 **열한 개**를 검증했고 **둘이 개선 가능했다.**

| 개선된 것 | 왜 남아 있었는가 | 이득 |
|---|---|---|
| 경험 구간 경계 (V156) | V154에서 **진단 표를 읽기 좋게** 고른 값이었다 | +1.33 |
| 피처 쪽 `RELIABILITY_SCALE` (V161) | V155가 **드리프트 쪽만** 검증하고 넘어갔다 | +1.05 |

**둘 다 근거가 가장 약한 쪽이었다.** 반면 하이퍼파라미터가 다른 성분과 정반대여서
가장 의심스러워 보였던 Context는 일곱 변형 전부 기각됐다(V157) —
**"튜닝되지 않았다"는 "잘못 튜닝되었다"가 아니다.**

---

## 6. 최종 상태

| | |
|---|---|
| **최고점** | `submit_v161.zip` · **1053.2326413884** · md5 `993d49754403a9586ad392ed091c0ea6` |
| 누적 | `1002.5720 → 1053.2326` = **+50.66** |
| 제출 | 8회 중 **6회 갱신**, 2회는 의도한 게이트 시험(V144·V151) |

### 판정 규칙 (제출 8회로 검증)

> **손실 폴드가 하나라도 있으면 제출하지 않는다.
> 모든 폴드가 이득이면 크기가 작아도 올린다.**
> 동률이면 3시즌 평균으로 가른다.

### 다음에 재개할 때 남은 것

| 항목 | 성질 |
|---|---|
| 하네스 사전확률 정리 | `add_row_features`의 `target_prior`가 V102는 폴드별, V111·V116·V130은 전역. **제출본은 규칙 위반 없음**(배포 시 전역 = 학습 전용). 폴드 추정치만 한 스칼라 낙관적이며, 새로 폴드 모델을 적합할 때 폴드별을 쓰면 해소된다 |
| 시즌 특이 구조 135~177점 | V148에서 실재 확인. 2025 라벨 없이는 걷을 수 없다 |
| 새 상수·축 | 알려진 것은 전부 검증됨. V159가 캐시된 78방향 국소 최적을 확인했다 |
