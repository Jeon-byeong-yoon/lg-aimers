# 18차 V17 기반 추가 상황별 OOF 잔차 보정

> 담당: 전병윤

## 기준선

- V17 Public Score: `883.9891184635`
- V17 OOF(95% tree + 5% Logistic)에서 잔차를 재계산하여 추가 보정 축 탐색

## 방법

- V17 blended OOF 예측으로 잔차를 새로 계산
- count·pitcher_count lookup도 V17 OOF 기준으로 재계산 (smoothing·가중치 동일 유지)
- 10개 상황 그룹 × 6 smoothing × 6 strength = 360개 단독 조합 탐색
- 두 시즌 모두 V17보다 개선된 후보만 허용 (단독 121개, 조합 10개 통과)
- 두 시즌의 최소 개선량을 최대화하는 조합 선택

선택된 추가 보정:

- 주자 배치 `base_state`: smoothing `10000`, 가중치 `1.0`
- 카운트×손잡이 `count_hand`: smoothing `50`, 가중치 `0.25`

## 시간순 검증

| 후보 | 2023 Brier | 2024 Brier |
| --- | ---: | ---: |
| V17 | 0.253357894 | 0.247850457 |
| V18 | 0.253343933 | 0.247833501 |
| 개선 | 0.000013960 | 0.000016956 |

두 시즌 모두 개선됐고 2024 개선 폭이 더 큽니다.

## 제출 후보

- `submit_v18.zip`
- V17과 동일한 트리+Logistic 구조, calibration 파일만 v18으로 교체
- 추가된 lookup: `base_state`, `count_hand` (OOF로 고정, 테스트 행 독립 처리)
